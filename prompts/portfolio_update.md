Return JSON matching the PortfolioPatch schema. Use only `update_project`, `add_skill`, `add_timeline_entry`, `suggest_resume`, and `suggest_blog`. Never delete fields or projects. Only update the mapped project. Skills must be demonstrated by code evidence and not duplicates. Timeline is only for MAJOR or MILESTONE changes. Resume/blog operations are suggestions and not executable. Keep all wording specific, accurate, professional, and concise.
